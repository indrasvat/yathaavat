repos:
  - repo: local
    hooks:
      - id: make-check
        name: make check (pre-push)
        entry: make check
        language: system
        pass_filenames: false
        stages: [pre-push]
